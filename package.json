{
  "name": "calculadora-caixas",
  "version": "1.0.0",
  "private": true,
  "description": "App web estatico para calcular distribuicao de produtos em caixas.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check service-worker.js",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/app-quality.test.js",
    "test:e2e": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-e2e.ps1"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
